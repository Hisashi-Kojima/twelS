name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master and develop branch
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "python-test"
  python-test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    env:
      # add '/home/runner/work/twelS/twelS/python/front' for pytest.
      PYTHONPATH: /home/runner/work/twelS/twelS/python:/home/runner/work/twelS/twelS/python/front
      
      # dummy values
      DJANGO_SECRET_KEY: foo
      MY_HISASHI_PASSWORD: foo
      MY_DB_NAME: foo
      DB_CONTAINER_NAME: foo
      SENDGRID_API_KEY: foo

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # This action check-out my repository under $GITHUB_WORKSPACE, 
      # so my workflow can access it.
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with: 
          python-version: '3.11.3'
          
      - name: Update pip
        run: pip install --upgrade pip

      - name: Install dependencies
        run: pip install -r python/requirements.txt
      
      - name: List Files
        run: |
          pwd
          ls
          echo '################'
          cd python
          ls
          echo '################'
          cd ../..
          ls

      - name: create a dummy file
        run: |
          mkdir python/front/logs
          touch python/front/logs/debug.log

      - name: check Django DEBUG = False
        run: pytest python/tests/test_settings.py

      - name: parser test
        run: pytest python/tests/test_parser.py

      - name: pathset test
        run: pytest python/tests/test_pathset.py

      - name: tree test
        run: pytest python/tests/test_tree.py
