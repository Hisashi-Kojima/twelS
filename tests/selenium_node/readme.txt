selenium_nodeディレクトリ
設定ファイルやログが配置されます
assetsの中身は追跡(gitにステージング)されません

selenium_node directory
Configuration files and logs are placed.
The contents of the assets directory are not tracked (not staged to git).

config.toml = docker-node config
assets = selenium server logs and sessionCapabilities

