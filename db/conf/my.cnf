# MySQL custom config
[mysqld]
# サーバ全体に適用される
character-set-server=utf8mb4
collation-server=utf8mb4_0900_as_cs
default-time-zone = '+00:00'  # UTC +0

[mysql]
default-character-set=utf8mb4

[mysqld_safe]
# エラーログの出力先
log-error=/var/log/mysql/error.log
